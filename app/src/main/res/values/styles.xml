<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@style/Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">@color/black4A</item>
        <item name="android:textSize">14sp</item>
    </style>

    <style name="BaseDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@color/transparent</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:background">@color/transparent</item>
        <!-- 背景色 -->
        <item name="android:windowBackground">@color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 模糊 -->
        <item name="android:windowFullscreen">true</item>
        <!-- 全屏 -->
    </style>

    <!--底部按钮-->
    <style name="radioButtonStyle">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <!--        <item name="android:paddingTop">@dimen/qb_px_7</item>-->
        <!--        <item name="android:paddingBottom">@dimen/qb_px_5</item>-->
        <item name="android:paddingTop">@dimen/qb_px_5</item>
        <item name="android:paddingBottom">@dimen/qb_px_5</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="lineStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/qb_px_1</item>
        <item name="android:background">@color/backgroundColor</item>
    </style>
</resources>
